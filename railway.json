{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DART",
    "buildCommand": "curl -fsSL https://gitee.com/mirrors/flutter/raw/main/flutter_install.sh | bash && flutter pub get && flutter build web --release",
    "installCommand": "curl -fsSL https://gitee.com/mirrors/flutter/raw/main/flutter_install.sh | bash"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
